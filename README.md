# FRRouting Documentation RAG System

RAG (Retrieval-Augmented Generation) система для работы с документацией FRRouting. Позволяет задавать вопросы на естественном языке и получать точные ответы с конкретными командами конфигурации.

## Возможности

- 🔍 **Умный поиск**: Гибридный поиск (семантический + ключевые слова)
- 🤖 **Генерация ответов**: Интеграция с OpenAI GPT и Google Gemini
- 📚 **Автоматический скрапинг**: Сбор документации FRRouting
- 🧩 **Chunking**: Разбиение документов на семантические фрагменты
- 🎯 **Адаптивный поиск**: Автоматическая настройка порога релевантности

## Быстрый старт

### 1. Установка

```bash
# Клонируйте репозиторий
git clone <your-repo-url>
cd fr-docs-scraper

# Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements.txt
```

### 2. Скрапинг документации

```bash
python scraper.py \
  --base-url https://docs.frrouting.org/en/latest/ \
  --output fr_docs \
  --delay 0.1 \
  --max-depth 6 \
  --log-file scraper.log
```

### 3. Разбиение на чанки

```bash
python chunking.py --input fr_docs --output chunks.json
```

### 4. Создание векторной БД

```bash
python vectorize.py --chunks chunks.json --output vector_db
```

### 5. Использование RAG системы

```bash
# Установите API ключ
export GEMINI_API_KEY='your-key'

# Задайте вопрос
python rag_query.py --query "How to configure BGP?" --model gemini
```

## Примеры использования

### Поиск конкретных команд

```bash
# Настройка IP адреса
python rag_query.py --query "ip address" --model gemini --min-relevance 0.05

# Конфигурация BGP
python rag_query.py --query "router bgp ASN configure" --model gemini

# С показом релевантности
python rag_query.py --query "OSPF setup" --model gemini --show-relevance
```

### Простой поиск (без генерации ответа)

```bash
python query_db.py --query "BGP neighbor" --n-results 5
```

## Структура проекта

```
fr-docs-scraper/
├── scraper.py          # Скрапер документации
├── chunking.py         # Разбиение на чанки
├── vectorize.py        # Создание векторной БД
├── query_db.py         # Простой поиск
├── rag_query.py        # RAG система (поиск + генерация)
├── test_gemini.py      # Тест Gemini API
├── requirements.txt    # Зависимости
├── README.md           # Этот файл
├── README_RAG.md       # Подробная документация RAG
└── QUICKSTART.md       # Быстрый старт
```

## Особенности поиска

### Гибридный поиск
- **Семантический поиск**: Понимает смысл запроса
- **Поиск по ключевым словам**: Находит точные совпадения команд

### Автоматическая адаптация
- Автоматически снижает порог релевантности, если результатов нет
- Адаптируется под качество поиска

### Query Expansion
- Автоматически расширяет технические термины
- "ip address" → "configure ip address on interface zebra command set"

## Требования

- Python 3.8+
- API ключ для OpenAI или Google Gemini (для генерации ответов)

## Лицензия

MIT

## Автор

Создано для работы с документацией FRRouting.

